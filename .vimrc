" Optional theme, comment if none on system.
colorscheme molokai

" Sets relative line numbering for easier navigation through file.
set relativenumber

" Set tab -> 4 spaces
set tabstop=4

" Syntax highlighting
syntax on
